# Git/Abstraction/Web scraping Intern Docs

## Installation

To install this repo, make sure to have npm installed.
Run `npm install yarn` and `yarn install` to install the required node packages.

##
